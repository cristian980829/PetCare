@model PetCare.Api.Models.DetailViewModel

@{
    ViewData["Title"] = "Create";
}

<h2>Crear</h2>

<h4>Detalle a la Historia Clínica</h4>
<hr />
<div class="row">
    <div class="col-md-12">
        <form asp-action="AddDetail" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ClinicalHistoryId" />

            <partial name="_Detail" />

            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
                <a asp-action="DetailsClinicalHistory" asp-route-id="@Model.ClinicalHistoryId" class="btn btn-success">Regresar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}